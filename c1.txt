<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meridian Builders — ERP</title>
    <script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link
        href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600&family=Syne:wght@500;600;700;800&display=swap"
        rel="stylesheet">

    <!-- Firebase SDKs -->
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js";
        import { getFirestore, doc, getDoc, setDoc, onSnapshot } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js";
        const firebaseConfig = {
            apiKey: "AIzaSyCkhf_MbNk-8GAqKBXCeJQ2pk1V_sLyzPU",
            authDomain: "meridian-construction.firebaseapp.com",
            projectId: "meridian-construction",
            storageBucket: "meridian-construction.firebasestorage.app",
            messagingSenderId: "1067477174772",
            appId: "1:1067477174772:web:2c5e69d117b626047591a1"
        };
        const app = initializeApp(firebaseConfig);
        const db = getFirestore(app);
        window.db = db; window.fsDoc = doc; window.fsGetDoc = getDoc;
        window.fsSetDoc = setDoc; window.fsOnSnapshot = onSnapshot;
        window.firebaseReady = true;
        window.dispatchEvent(new Event("firebaseReady"));
    </script>

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0
        }

        :root {
            --navy: #1a1f2b;
            --navy2: #232a3b;
            --navy3: #1e2537;
            --teal: #00d1c1;
            --teal-glow: rgba(0, 209, 193, 0.12);
            --teal-dim: rgba(0, 209, 193, 0.5);
            --teal-border: rgba(0, 209, 193, 0.25);
            --bright: #f0f4ff;
            --muted: #8892a4;
            --dim: #5a6478;
            --border: rgba(255, 255, 255, 0.07);
            --border2: rgba(255, 255, 255, 0.12);
            --bg-main: #0f1318;
            --bg-card: #181e2a;
            --bg-card2: #1c2333;
            --green: #10b981;
            --green-glow: rgba(16, 185, 129, 0.12);
            --red: #f43f5e;
            --red-glow: rgba(244, 63, 94, 0.12);
            --amber: #f59e0b;
            --amber-glow: rgba(245, 158, 11, 0.12);
            --blue: #3b82f6;
            --blue-glow: rgba(59, 130, 246, 0.12);
        }

        html,
        body {
            height: 100%;
            font-family: 'DM Sans', sans-serif;
            background: var(--bg-main);
            color: var(--bright);
            font-size: 13px;
            overflow: hidden
        }

        /* ── LOCK SCREEN ── */
        #lockScreen {
            position: fixed;
            inset: 0;
            background: linear-gradient(135deg, #0a0e15 0%, #1a1f2b 60%, #0d1520 100%);
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center
        }

        #lockScreen.hidden {
            display: none
        }

        .lock-box {
            background: var(--bg-card);
            border: 1px solid var(--border2);
            border-radius: 20px;
            padding: 40px 36px 32px;
            width: 360px;
            max-width: 94vw;
            box-shadow: 0 24px 64px rgba(0, 0, 0, 0.6), 0 0 0 1px rgba(0, 209, 193, 0.08);
            text-align: center;
            animation: fadeUp .3s ease
        }

        @keyframes fadeUp {
            from {
                opacity: 0;
                transform: translateY(16px)
            }

            to {
                opacity: 1;
                transform: translateY(0)
            }
        }

        .lock-logo {
            width: 64px;
            height: 64px;
            border-radius: 18px;
            background: linear-gradient(135deg, #00d1c1, #0097a7);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 16px;
            box-shadow: 0 0 32px rgba(0, 209, 193, 0.4);
            font-size: 28px
        }

        .lock-box h2 {
            font-family: 'Syne', sans-serif;
            font-size: 20px;
            font-weight: 700;
            color: var(--bright);
            margin-bottom: 4px
        }

        .lock-box p {
            font-size: 12px;
            color: var(--muted);
            margin-bottom: 24px
        }

        .lock-input-wrap {
            position: relative;
            margin-bottom: 12px
        }

        #lockInput {
            width: 100%;
            padding: 12px 44px 12px 14px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid var(--border2);
            border-radius: 10px;
            font-size: 15px;
            font-family: 'DM Sans', sans-serif;
            color: var(--bright);
            letter-spacing: 3px;
            transition: border-color .15s, box-shadow .15s
        }

        #lockInput:focus {
            outline: none;
            border-color: var(--teal);
            box-shadow: 0 0 0 3px var(--teal-glow)
        }

        #lockInput.error {
            border-color: var(--red);
            box-shadow: 0 0 0 3px rgba(244, 63, 94, .15);
            animation: shake .35s ease
        }

        .lock-eye {
            position: absolute;
            right: 12px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            cursor: pointer;
            font-size: 16px;
            color: var(--muted);
            transition: color .15s
        }

        .lock-eye:hover {
            color: var(--bright)
        }

        #lockBtn {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, #00d1c1, #0097a7);
            color: #0a1525;
            border: none;
            border-radius: 10px;
            font-size: 14px;
            font-weight: 700;
            font-family: 'Syne', sans-serif;
            cursor: pointer;
            transition: opacity .15s, transform .15s;
            letter-spacing: .5px
        }

        #lockBtn:hover {
            opacity: .9
        }

        #lockBtn:active {
            transform: scale(.98)
        }

        #lockErr {
            font-size: 11px;
            color: var(--red);
            margin-top: 10px;
            min-height: 16px;
            font-weight: 600
        }

        @keyframes shake {

            0%,
            100% {
                transform: translateX(0)
            }

            20% {
                transform: translateX(-7px)
            }

            40% {
                transform: translateX(7px)
            }

            60% {
                transform: translateX(-5px)
            }

            80% {
                transform: translateX(5px)
            }
        }

        /* ── APP LAYOUT ── */
        #appWrap {
            display: none;
            height: 100vh;
            flex-direction: row
        }

        #appWrap.show {
            display: flex
        }

        .sidebar {
            width: 260px;
            min-width: 260px;
            background: var(--navy);
            border-right: 1px solid var(--border);
            display: flex;
            flex-direction: column;
            transition: width .3s cubic-bezier(.4, 0, .2, 1), min-width .3s;
            overflow: hidden;
            flex-shrink: 0
        }

        .sidebar.collapsed {
            width: 64px;
            min-width: 64px
        }

        .app-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            background: var(--bg-main)
        }

        /* ── SIDEBAR PIECES ── */
        .sb-logo {
            height: 64px;
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 0 15px;
            flex-shrink: 0
        }

        .sb-logo-icon {
            width: 36px;
            height: 36px;
            min-width: 36px;
            border-radius: 10px;
            background: linear-gradient(135deg, #00d1c1, #0097a7);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            box-shadow: 0 0 16px rgba(0, 209, 193, 0.3);
            animation: pulse 3s ease-in-out infinite;
            flex-shrink: 0
        }

        @keyframes pulse {

            0%,
            100% {
                opacity: 1
            }

            50% {
                opacity: .75
            }
        }

        .sb-logo-text {
            overflow: hidden;
            white-space: nowrap;
            transition: opacity .2s
        }

        .sb-logo-text h1 {
            font-family: 'Syne', sans-serif;
            font-size: 14px;
            font-weight: 700;
            color: var(--bright);
            line-height: 1
        }

        .sb-logo-text p {
            font-size: 9px;
            color: var(--teal);
            font-weight: 600;
            letter-spacing: .1em;
            margin-top: 2px
        }

        .sidebar.collapsed .sb-logo-text {
            opacity: 0;
            width: 0
        }

        .sb-toggle {
            padding: 8px 10px;
            border-bottom: 1px solid var(--border);
            flex-shrink: 0
        }

        .sb-toggle-btn {
            width: 100%;
            height: 32px;
            background: rgba(255, 255, 255, 0.04);
            border: 1px solid var(--border);
            border-radius: 8px;
            cursor: pointer;
            color: var(--muted);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            font-family: 'DM Sans', sans-serif;
            font-size: 11px;
            font-weight: 500;
            transition: background .15s, color .15s
        }

        .sb-toggle-btn:hover {
            background: var(--navy2);
            color: var(--bright)
        }

        .sb-section-label {
            padding: 14px 14px 4px;
            font-size: 10px;
            font-weight: 600;
            letter-spacing: .1em;
            color: var(--dim);
            text-transform: uppercase;
            white-space: nowrap;
            overflow: hidden;
            transition: opacity .2s
        }

        .sidebar.collapsed .sb-section-label {
            opacity: 0
        }

        .sb-nav {
            flex: 1;
            padding: 4px 8px 8px;
            overflow-y: auto;
            overflow-x: hidden
        }

        .sidebar.collapsed .sb-nav {
            padding: 8px 0
        }

        .sb-nav::-webkit-scrollbar {
            width: 3px
        }

        .sb-nav::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, .1);
            border-radius: 99px
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 10px;
            border-radius: 8px;
            cursor: pointer;
            color: var(--muted);
            position: relative;
            transition: background .18s, color .18s;
            margin-bottom: 1px;
            user-select: none;
            white-space: nowrap
        }

        .sidebar.collapsed .nav-item {
            padding: 10px 0;
            border-radius: 0;
            justify-content: center
        }

        .nav-item:hover {
            background: var(--navy2)
        }

        .nav-item.active {
            background: var(--teal-glow);
            color: var(--teal)
        }

        .nav-item.active .nav-activebar {
            position: absolute;
            left: 0;
            top: 20%;
            height: 60%;
            width: 3px;
            border-radius: 0 3px 3px 0;
            background: var(--teal);
            box-shadow: 0 0 8px var(--teal-dim)
        }

        .sidebar.collapsed .nav-item.active .nav-activebar {
            display: none
        }

        .nav-label {
            flex: 1;
            font-size: 13px;
            font-weight: 500;
            transition: opacity .2s;
            overflow: hidden
        }

        .sidebar.collapsed .nav-label {
            opacity: 0;
            width: 0;
            flex: 0
        }

        .nav-chevron {
            transition: transform .28s cubic-bezier(.4, 0, .2, 1);
            flex-shrink: 0
        }

        .nav-chevron.open {
            transform: rotate(90deg)
        }

        .sidebar.collapsed .nav-chevron {
            display: none
        }

        .nav-dot {
            width: 5px;
            height: 5px;
            border-radius: 50%;
            background: var(--teal);
            box-shadow: 0 0 7px var(--teal-dim);
            flex-shrink: 0
        }

        .sidebar.collapsed .nav-dot {
            display: none
        }

        .nav-tooltip {
            position: absolute;
            left: calc(100% + 10px);
            top: 50%;
            transform: translateY(-50%);
            background: #2d3548;
            color: var(--bright);
            font-size: 11px;
            font-weight: 500;
            padding: 5px 10px;
            border-radius: 6px;
            white-space: nowrap;
            pointer-events: none;
            opacity: 0;
            transition: opacity .15s;
            z-index: 200;
            border: 1px solid rgba(255, 255, 255, .1)
        }

        .sidebar.collapsed .nav-item:hover .nav-tooltip {
            opacity: 1
        }

        .nav-accordion {
            overflow: hidden;
            transition: max-height .32s cubic-bezier(.4, 0, .2, 1), opacity .28s ease;
            max-height: 0;
            opacity: 0
        }

        .nav-accordion.open {
            max-height: 400px;
            opacity: 1
        }

        .sidebar.collapsed .nav-accordion {
            display: none
        }

        .nav-child {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 6px 10px 6px 12px;
            border-radius: 7px;
            cursor: pointer;
            color: var(--dim);
            transition: background .15s, color .15s;
            margin-bottom: 1px;
            font-size: 12px;
            font-weight: 400;
            white-space: nowrap
        }

        .nav-child:hover {
            background: rgba(255, 255, 255, .04);
            color: var(--bright)
        }

        .nav-child.active {
            color: var(--teal);
            background: var(--teal-glow);
            font-weight: 500
        }

        .nav-child-line {
            width: 14px;
            height: 1px;
            background: var(--border2);
            flex-shrink: 0
        }

        .nav-child.active .nav-child-line {
            background: var(--teal)
        }

        .nav-child-dot {
            width: 4px;
            height: 4px;
            border-radius: 50%;
            background: var(--teal);
            box-shadow: 0 0 5px var(--teal-dim);
            margin-left: auto;
            flex-shrink: 0
        }

        .sb-profile {
            border-top: 1px solid var(--border);
            padding: 12px;
            flex-shrink: 0
        }

        .profile-row {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 10px;
            border-radius: 8px;
            cursor: pointer;
            transition: background .15s
        }

        .sidebar.collapsed .profile-row {
            justify-content: center;
            padding: 8px 0
        }

        .profile-row:hover {
            background: var(--navy2)
        }

        .profile-avatar {
            width: 32px;
            height: 32px;
            min-width: 32px;
            border-radius: 50%;
            background: linear-gradient(135deg, #00d1c1 20%, #0097a7);
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Syne', sans-serif;
            font-size: 11px;
            font-weight: 700;
            color: #0a1525;
            flex-shrink: 0
        }

        .profile-info {
            overflow: hidden;
            white-space: nowrap;
            transition: opacity .2s
        }

        .sidebar.collapsed .profile-info {
            opacity: 0;
            width: 0
        }

        .profile-info p:first-child {
            font-size: 12px;
            font-weight: 600;
            color: var(--bright)
        }

        .profile-info p:last-child {
            font-size: 10px;
            color: var(--dim);
            margin-top: 1px
        }

        /* ── TOPBAR ── */
        .topbar {
            height: 56px;
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            padding: 0 20px;
            gap: 12px;
            flex-shrink: 0;
            background: var(--navy)
        }

        .topbar-title {
            flex: 1
        }

        .topbar-title h2 {
            font-family: 'Syne', sans-serif;
            font-size: 15px;
            font-weight: 700;
            color: var(--bright)
        }

        .topbar-title p {
            font-size: 11px;
            color: var(--dim);
            margin-top: 1px
        }

        .cloud-pill {
            padding: 4px 12px;
            border-radius: 99px;
            font-size: 11px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 5px
        }

        .cloud-synced {
            background: rgba(16, 185, 129, .15);
            color: #10b981;
            border: 1px solid rgba(16, 185, 129, .3)
        }

        .cloud-syncing {
            background: rgba(245, 158, 11, .12);
            color: var(--amber);
            border: 1px solid rgba(245, 158, 11, .3)
        }

        .cloud-offline {
            background: rgba(255, 255, 255, .05);
            color: var(--muted);
            border: 1px solid var(--border)
        }

        .cloud-error {
            background: var(--red-glow);
            color: var(--red);
            border: 1px solid rgba(244, 63, 94, .3)
        }

        @keyframes spin {
            to {
                transform: rotate(360deg)
            }
        }

        .spin {
            animation: spin 1s linear infinite;
            display: inline-block
        }

        /* ── BUTTONS ── */
        .btn {
            padding: 7px 14px;
            border-radius: 8px;
            font-size: 12px;
            font-weight: 600;
            cursor: pointer;
            border: none;
            font-family: 'DM Sans', sans-serif;
            transition: opacity .15s, transform .1s;
            display: inline-flex;
            align-items: center;
            gap: 6px
        }

        .btn:active {
            transform: scale(.97)
        }

        .btn:hover {
            opacity: .88
        }

        .btn-teal {
            background: linear-gradient(135deg, #00d1c1, #0097a7);
            color: #0a1525
        }

        .btn-green {
            background: rgba(16, 185, 129, .15);
            color: #10b981;
            border: 1px solid rgba(16, 185, 129, .3)
        }

        .btn-green:hover {
            background: rgba(16, 185, 129, .25);
            opacity: 1
        }

        .btn-red {
            background: rgba(244, 63, 94, .12);
            color: var(--red);
            border: 1px solid rgba(244, 63, 94, .25)
        }

        .btn-red:hover {
            background: rgba(244, 63, 94, .22);
            opacity: 1
        }

        .btn-amber {
            background: rgba(245, 158, 11, .12);
            color: var(--amber);
            border: 1px solid rgba(245, 158, 11, .25)
        }

        .btn-amber:hover {
            background: rgba(245, 158, 11, .22);
            opacity: 1
        }

        .btn-blue {
            background: rgba(59, 130, 246, .12);
            color: var(--blue);
            border: 1px solid rgba(59, 130, 246, .25)
        }

        .btn-blue:hover {
            background: rgba(59, 130, 246, .22);
            opacity: 1
        }

        .btn-ghost {
            background: rgba(255, 255, 255, .06);
            color: var(--muted);
            border: 1px solid var(--border)
        }

        .btn-ghost:hover {
            background: rgba(255, 255, 255, .1);
            color: var(--bright);
            opacity: 1
        }

        .btn-sm {
            padding: 5px 10px;
            font-size: 11px;
            border-radius: 6px
        }

        .btn-icon {
            padding: 6px;
            width: 30px;
            height: 30px;
            justify-content: center;
            border-radius: 7px
        }

        /* ── MAIN VIEWS ── */
        .view {
            display: none;
            flex: 1;
            overflow-y: auto;
            padding: 20px
        }

        .view.on {
            display: block
        }

        .view::-webkit-scrollbar {
            width: 4px
        }

        .view::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, .1);
            border-radius: 99px
        }

        /* ── CARDS ── */
        .stat-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 12px;
            margin-bottom: 20px
        }

        .stat-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 16px;
            transition: box-shadow .2s, transform .2s;
            position: relative;
            overflow: hidden
        }

        .stat-card::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px
        }

        .stat-card.green::before {
            background: linear-gradient(90deg, #10b981, transparent)
        }

        .stat-card.red::before {
            background: linear-gradient(90deg, #f43f5e, transparent)
        }

        .stat-card.amber::before {
            background: linear-gradient(90deg, #f59e0b, transparent)
        }

        .stat-card.blue::before {
            background: linear-gradient(90deg, #3b82f6, transparent)
        }

        .stat-card:hover {
            box-shadow: 0 8px 24px rgba(0, 0, 0, .3);
            transform: translateY(-2px)
        }

        .stat-label {
            font-size: 10px;
            font-weight: 600;
            letter-spacing: .08em;
            text-transform: uppercase;
            color: var(--dim);
            margin-bottom: 8px
        }

        .stat-value {
            font-family: 'Syne', sans-serif;
            font-size: 22px;
            font-weight: 700
        }